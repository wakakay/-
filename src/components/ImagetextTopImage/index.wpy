<template>
    <view class="imagetextTopImage-article">
        <view class="imagetextTopImage-article-img {{isActived ? 'on' : ''}}" :class="{'image-loading': !imageLoaded}">
            <image class="imagetextTopImage-article-img" @load="handleImageLoaded" src="{{bundle.bigimage}}" hidden="{{!imageLoaded}}" mode="widthFix"></image>
        </view>
        <view class="imagetextTopImage-article-title-row {{bundle.layoutStatus.titleAlignLeft?'title-left':'title-center'}} {{isActived ? 'on' : ''}}">
            <image class="imagetextTopImage-article-title-icon" wx:if="{{bundle.layoutStatus.titleIconShow}}" src="{{bundle.layoutStatus.titleIconUrl}}" mode="scaleToFill"/>
            <view class="imagetextTopImage-article-title-text">{{bundle.title}}</view>
        </view>
        <view  class="imagetextTopImage-article-desc-m {{bundle.description.length>34?'font-size-s':'font-size-b'}} {{isActived ? 'on' : ''}}">
            <rich-text nodes="{{bundle.richTextNodeList}}"></rich-text>
        </view>
        <view class="imagetextTopImage-article-point-text {{isActived ? 'on' : ''}}" wx:if="{{bundle.navigation.point.text}}">{{bundle.navigation.point.text}}</view>
    </view>
</template>

<script>
    import wepy from 'wepy'
    import loadingMixin from '../../mixins/loading'
    export default class ImagetextTopImage extends wepy.component {
        mixins = [loadingMixin] // end mixins
        props = {
            offset: {},
            bundle: {
                type: Object
            },
            isActived: {
              type: Boolean
            }
        }
        data = {}
        methods = {
        }
    }
</script>

<style lang="less">
    .imagetextTopImage-article {
        height: 100%;
        width: 100%;
        background-color: #FFF;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .imagetextTopImage-article-img.on {
        animation: zoomIn .3s ease-in 0s;
    }
    .imagetextTopImage-article-title-row.on {
        animation: zoomIn .3s ease-in .05s;
    }
    .imagetextTopImage-article-desc-m.on {
        animation: zoomIn .3s ease-in .1s;
    }
    .imagetextTopImage-article-point-text.on {
        animation: zoomIn .3s ease-in .15s;
    }

    .title-left{
        justify-content: flex-start;
    }
    .title-center{
        justify-content: center;
    }
    .font-size-s{
        font-size: 32rpx;
    }
    .font-size-b{
        font-size: 36rpx;
    }
    .imagetextTopImage-article-title-row {
        margin: 48rpx 72rpx 48rpx 72rpx;
        display: flex;
    }
    .imagetextTopImage-article-title-icon{
        width: 64rpx;
        height: 64rpx;
        margin-right: 16rpx;

    }
    .imagetextTopImage-article-title-text{
        color: #3fa790;
        font-family: 'PingFangSC-Semibold';
        font-size: 44rpx;
        line-height: 56rpx;
        word-break: break-all;
        white-space: pre-line;
    }
    .imagetextTopImage-article-img{
        width: 100%;
    }
    .imagetextTopImage-article-desc-m{
        margin: 0 72rpx 0 72rpx;
        color: #2c3f49;
        font-family: 'PingFangSC-Regular';
        text-align: justify;
        line-height: 64rpx;
        word-break: break-all;
        white-space: pre-line;
        /* font-size: 40rpx; */
    }
    .imagetextTopImage-article-point-text{
        color: #303f48;
        font-family: 'PingFangSC-Medium';
        font-size: 40rpx;
        line-height: 56rpx;
        margin: 80rpx 72rpx 0rpx 72rpx;
        word-break: break-all;
        white-space: pre-line;
    }

</style>
